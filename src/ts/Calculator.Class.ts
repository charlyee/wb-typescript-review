class Calculator {
  constructor () {
    const form: HTMLElement = document.querySelector( '#calculator' );
    const num1: HTMLInputElement = document.querySelector( '[name="num1"]' );
    const num2: HTMLInputElement = document.querySelector( '[name="num2"]' );
    const result: HTMLElement = document.querySelector( '#calculator strong' );
  }
  
}